# ---------------------------------
# Python / uv / Jupyter
# ---------------------------------

# uvやvenvで作成される仮想環境フォルダ
# (uv venv で .venv が作成された場合)
/.venv/
# (古いvenv)
/venv/
/env/

# Pythonのバイトコードとキャッシュ
__pycache__/
*.pyc
*.pyo
*.pyd

# Jupyter Notebookのチェックポイント
.ipynb_checkpoints/

# Pythonのパッケージング・ビルド生成物
/dist/
/build/
/*.egg-info/

# ---------------------------------
# Rust / Cargo
# ---------------------------------

# Rustのビルド生成物フォルダ (最重要)
# ワークスペースのルート(rust_projects/target) と ルート (target) の両方を指定
/rust_projects/target/
/target/

# Cargoのロックファイル (勉強会では全員の環境を一致させるため
# Cargo.lock は *コミットする* ことを推奨します。
# もしライブラリとして開発し、ロックファイルを無視したい場合は
# /Cargo.lock をここに追加します)

# ---------------------------------
# VSCode / Devcontainer
# ---------------------------------

# VSCodeのローカル（ユーザー固有）設定
# .devcontainer/ フォルダは *コミITTする必要があります*
.vscode/

# VSCodeのワークスペースファイル
*.code-workspace

# ---------------------------------
# OS / 一般
# ---------------------------------

# macOS
.DS_Store
._*

# Windows
Thumbs.db
desktop.ini

# ログファイル
*.log

# ---------------------------------
# プロジェクト固有
# ---------------------------------

# (例) ダウンロードした大規模データセット
# data/ はコミットするが、その中の重いzipファイルなどは無視する
data/*.zip
data/*.gz

# (例) ローカルで訓練したモデルファイル
/models/
*.pth
*.safetensors
*.onnx